# Server-only LLM configuration for Edge Functions / local dev generation.
# NEVER copy these into apps/mobile or prefix with EXPO_PUBLIC_.

LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
