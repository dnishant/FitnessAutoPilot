# E2E placeholder — full Detox/Maestro suite is intentionally deferred.
# Local verification for this slice:
# 1) pnpm test:unit
# 2) pnpm --filter @fitness-autopilot/mobile web
# 3) Walk auth → onboarding → goal → generate → Today
